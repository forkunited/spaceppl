
var f = cache(function(i) {
	if (i == 0) {
		return 0;
	} else {
		return i + g(i-1);
	}
}, 10);

var g = cache(function(i) {
    if (i == 0) {
        return 0;
    } else {
        return i + f(i-1);
    }
}, 10);

f(10);

